"use strict" // treat all js code as newer version

// console.log(3+3);
// console.log("amit");

// let name = "amosh";
// let age =18;
// let isLoggedIn =false



//-----------------------------------------//
// Data types

// 1- primivite  (stack memory is used )
// 7 types : string , number , boolean , null, undefined, symbol,

const id= Symbol("123")
const anotherId = Symbol("123")
console.log(id === anotherId);


//-------------------------------------------------------------------------------------------------------------------------//
// memory in js 

// stack (primitive )  : when we keep value in stack we get copy

// heap (non - primitive ) : when we keep any value in heap we get reference

